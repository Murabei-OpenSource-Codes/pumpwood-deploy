apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-bigdata-corp-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-bigdata-corp-postgres
    spec:
      volumes:
      - name: crawler-bigdata-corp-data
        persistentVolumeClaim:
          claimName: crawler-bigdata-corp-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-bigdata-corp
      containers:
      - name: crawler-bigdata-corp-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-bigdata-corp
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-bigdata-corp
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-bigdata-corp-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
