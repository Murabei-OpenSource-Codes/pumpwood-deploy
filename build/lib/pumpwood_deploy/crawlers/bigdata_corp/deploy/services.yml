apiVersion : "v1"
kind: Service
metadata:
  name: crawler-bigdata-corp-postgres
  labels:
    type: db
    db: crawler-bigdata-corp-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-bigdata-corp-postgres
