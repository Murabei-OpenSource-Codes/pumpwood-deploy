apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-google-ads-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-google-ads-postgres
    spec:
      volumes:
      - name: crawler-google-ads-data
        persistentVolumeClaim:
          claimName: crawler-google-ads-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-google-ads
      containers:
      - name: crawler-google-ads-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-google-ads
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-google-ads
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-google-ads-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
