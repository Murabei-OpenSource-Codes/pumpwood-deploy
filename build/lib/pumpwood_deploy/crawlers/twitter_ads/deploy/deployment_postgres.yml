apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-twitter-ads-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-twitter-ads-postgres
    spec:
      volumes:
      - name: crawler-twitter-ads-data
        persistentVolumeClaim:
          claimName: crawler-twitter-ads-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-twitter-ads
      containers:
      - name: crawler-twitter-ads-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-twitter-ads
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-twitter-ads
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-twitter-ads-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
