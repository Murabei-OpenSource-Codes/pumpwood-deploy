apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-twitter-open-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-twitter-open-postgres
    spec:
      volumes:
      - name: crawler-twitter-open-data
        persistentVolumeClaim:
          claimName: crawler-twitter-open-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-twitter-open
      containers:
      - name: crawler-twitter-open-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-twitter-open
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-twitter-open
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-twitter-open-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
