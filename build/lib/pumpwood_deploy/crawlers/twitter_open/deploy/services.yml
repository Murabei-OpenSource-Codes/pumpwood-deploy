apiVersion : "v1"
kind: Service
metadata:
  name: crawler-twitter-open-postgres
  labels:
    type: db
    db: crawler-twitter-open-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-twitter-open-postgres
