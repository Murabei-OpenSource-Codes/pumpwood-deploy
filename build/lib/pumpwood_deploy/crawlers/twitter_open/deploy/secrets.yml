apiVersion: v1
kind: Secret
metadata:
  name: crawler-twitter-open
type: Opaque
data:
  db_username: {db_username}
  db_password: {db_password}
  microservice_username: {micro_username}
  microservice_password: {micro_password}
  app_consumer_key: {app_consumer_key}
  app_consumer_secret: {app_consumer_secret}
  ssl_key: {ssl_key}
  ssl_crt: {ssl_crt}
