apiVersion: apps/v1
kind: Deployment
metadata:
  name: facebook-graph-pusher
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: facebook-graph-pusher
    spec:
      volumes:
      - name: bucket-key
        secret:
          secretName: bucket-key
      - name: facebook-open-datalake-integration
        configMap:
          name: facebook-open-datalake-integration
      containers:
      - name: facebook-graph-pusher
        image: {repository}/facebook-graph-api-pusher:{version}
        imagePullPolicy: Always
        volumeMounts:
        - name: bucket-key
          readOnly: true
          mountPath: /etc/secrets
        - name: facebook-open-datalake-integration
          mountPath: /pusher/settings/datalake_integration.py
          subPath: datalake_integration.py
        env:
        #DATABASE
        - name: DB_HOST
          value: postgres-facebook-graph
        - name: DB_PORT
          value: "5432"
        - name: DB_DATABASE
          value: "crawler"
        - name: DB_DIALECT
          value: "postgres"
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: facebook-graph
              key: db_username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: facebook-graph
              key: db_password
        #Auth and Storage
        - name: AUTH_SERVER
          value: "http://haproxy-load-balancer:80/"
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: "/etc/secrets/key-storage.json"
        #RABBITMQ
        - name: RABBITMQ_QUEUE
          value: "facebook-graph-pusher"
        - name: RABBITMQ_HOST
          value: "rabbitmq-main"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USERNAME
          valueFrom:
            secretKeyRef:
              name: rabbitmq-main-secrets
              key: username
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-main-secrets
              key: password
        #Microsservice
        - name: MICROSERVICE_NAME
          value: 'facebook-graph-pusher'
        - name: MICROSERVICE_URL
          value: 'http://haproxy-load-balancer:80'
        - name: MICROSERVICE_USERNAME
          valueFrom:
              secretKeyRef:
                name: facebook-graph
                key: microservice_username
        - name: MICROSERVICE_PASSWORD
          valueFrom:
              secretKeyRef:
                name: facebook-graph
                key: microservice_password
        
