apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-google-trends-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-google-trends-postgres
    spec:
      volumes:
      - name: crawler-google-trends-data
        persistentVolumeClaim:
          claimName: crawler-google-trends-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-google-trends
      containers:
      - name: crawler-google-trends-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-google-trends
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-google-trends
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-google-trends-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
