apiVersion : "v1"
kind: Service
metadata:
  name: crawler-google-trends-postgres
  labels:
    type: db
    db: crawler-google-trends-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-google-trends-postgres
