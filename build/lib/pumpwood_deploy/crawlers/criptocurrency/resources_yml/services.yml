apiVersion : "v1"
kind: Service
metadata:
  name: crawler-cryptocurrency-postgres
  labels:
    type: db
    db: crawler-cryptocurrency-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-cryptocurrency-postgres
