apiVersion : "v1"
kind: Service
metadata:
  name: crawler-facebook-ads-postgres
  labels:
    type: db
    db: crawler-facebook-ads-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-facebook-ads-postgres
