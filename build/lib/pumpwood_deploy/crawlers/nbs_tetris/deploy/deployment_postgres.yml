apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-nbs-tetris-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: db
        db: crawler-nbs-tetris-postgres
    spec:
      volumes:
      - name: crawler-nbs-tetris-data
        persistentVolumeClaim:
          claimName: crawler-nbs-tetris-postgres
      - name: postgres-init-configmap
        configMap:
          name: postgres-init-configmap
      - name: secrets
        secret:
          secretName: crawler-nbs-tetris
      containers:
      - name: crawler-nbs-tetris-postgres
        image: postgres:10.3
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "10m"
          limits:
            cpu: "2"
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: crawler-nbs-tetris
              key: db_username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crawler-nbs-tetris
              key: db_password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: crawler-nbs-tetris-data
          mountPath: /var/lib/postgresql/data/
        - name: postgres-init-configmap
          mountPath: /docker-entrypoint-initdb.d/
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
