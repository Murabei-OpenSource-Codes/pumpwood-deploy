apiVersion : "v1"
kind: Service
metadata:
  name: crawler-nbs-tetris-postgres
  labels:
    type: db
    db: crawler-nbs-tetris-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: crawler-nbs-tetris-postgres
