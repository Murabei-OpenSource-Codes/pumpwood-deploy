apiVersion : "v1"
kind: Service
metadata:
  name: pumpwood-prediction-app
  labels:
    type: pumpwood-prediction-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: pumpwood-prediction-app
