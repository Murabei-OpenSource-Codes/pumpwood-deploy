apiVersion : "v1"
kind: Service
metadata:
  name: pumpwood-datalake-app
  labels:
    type: pumpwood-datalake-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: pumpwood-datalake-app
