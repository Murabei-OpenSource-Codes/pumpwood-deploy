apiVersion : "v1"
kind: Service
metadata:
  name: crawler-cryptocurrency-app
  labels:
    type: crawler-cryptocurrency-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: crawler-cryptocurrency-app
