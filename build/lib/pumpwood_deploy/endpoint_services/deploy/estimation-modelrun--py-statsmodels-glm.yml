apiVersion : "v1"
kind: Service
metadata:
  name: estimation-modelrun--py-statsmodels-glm
  labels:
      type: pumpwood-estimation-modelrun
      model_type: py-statsmodels-glm
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
      type: pumpwood-estimation-modelrun
      model_type: py-statsmodels-glm
