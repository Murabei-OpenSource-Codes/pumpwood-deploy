apiVersion : "v1"
kind: Service
metadata:
  name: facebook-graph-queue-manager
  labels:
    type: facebook-graph-queue-manager
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: facebook-graph-queue-manager
