apiVersion : "v1"
kind: Service
metadata:
  name: crawler-nbs-tetris-app
  labels:
    type: crawler-nbs-tetris-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: crawler-nbs-tetris-app
