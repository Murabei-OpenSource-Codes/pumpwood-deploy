apiVersion : "v1"
kind: Service
metadata:
  name: crawler-google-ads-app
  labels:
    type: crawler-google-ads-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: crawler-google-ads-app
