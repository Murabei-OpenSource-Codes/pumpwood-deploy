apiVersion : "v1"
kind: Service
metadata:
  name: pumpwood-frontend
  labels:
    type: pumpwood-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    type: pumpwood-frontend
