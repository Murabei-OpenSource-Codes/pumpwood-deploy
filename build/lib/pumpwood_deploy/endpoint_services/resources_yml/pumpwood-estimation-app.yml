apiVersion : "v1"
kind: Service
metadata:
  name: pumpwood-estimation-app
  labels:
    type: pumpwood-estimation-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: pumpwood-estimation-app
