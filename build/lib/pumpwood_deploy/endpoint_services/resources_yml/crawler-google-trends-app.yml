apiVersion : "v1"
kind: Service
metadata:
  name: crawler-google-trends-app
  labels:
    type: crawler-google-trends-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: crawler-google-trends-app
