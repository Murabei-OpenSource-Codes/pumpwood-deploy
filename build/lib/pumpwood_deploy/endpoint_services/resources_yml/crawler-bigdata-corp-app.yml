apiVersion : "v1"
kind: Service
metadata:
  name: crawler-bigdata-corp-app
  labels:
    type: crawler-bigdata-corp-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
  selector:
    type: crawler-bigdata-corp-app
