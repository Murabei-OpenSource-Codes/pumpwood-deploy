apiVersion : "v1"
kind: Service
metadata:
  name: postgres-pumpwood-datalake
  labels:
    type: db
    db: postgres-pumpwood-datalake
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: postgres-pumpwood-datalake
