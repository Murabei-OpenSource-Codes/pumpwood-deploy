apiVersion : "v1"
kind: Service
metadata:
  name: postgres-pumpwood-auth
  labels:
    type: db
    db: postgres-pumpwood-auth
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: postgres-pumpwood-auth
