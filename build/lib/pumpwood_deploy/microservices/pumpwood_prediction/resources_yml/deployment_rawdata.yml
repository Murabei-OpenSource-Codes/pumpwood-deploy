apiVersion: apps/v1
kind: Deployment
metadata:
  name: pumpwood-prediction-rawdata-workers
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: pumpwood-prediction-rawdata-workers
    spec:
      imagePullSecrets:
        - name: dockercfg
      volumes:
      - name: bucket-key
        secret:
          secretName: bucket-key
      containers:
      - name: pumpwood-prediction-rawdata-workers
        image: {repository}/pumpwood-prediction-rawdata-worker:{version}
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "1m"
        volumeMounts:
          - name: bucket-key
            readOnly: true
            mountPath: /etc/secrets
        env:
        # DATALAKE DATALAKE
        - name: DATALAKE_DB_HOST
          value: "postgres-pumpwood-datalake"
        - name: DATALAKE_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: pumpwood-datalake
              key: db_password

        # DATALAKE PREDICTION
        - name: PREDICTION_DB_HOST
          value: "postgres-pumpwood-prediction"
        - name: PREDICTION_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: pumpwood-prediction
              key: db_password

        #Google
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: "/etc/secrets/key-storage.json"
        - name: STORAGE_BUCKET_NAME
          value: {bucket_name}
        - name: STORAGE_TYPE
          value: 'google_bucket'

        # RABBITMQ QUEUE
        - name: RABBITMQ_HOST
          value: "rabbitmq-main"
        - name: RABBITMQ_USERNAME
          valueFrom:
            secretKeyRef:
              name: rabbitmq-main-secrets
              key: username
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-main-secrets
              key: password

        #Microsservice
        - name: MICROSERVICE_NAME
          value: 'pumpwood-prediction-rawdata'
        - name: MICROSERVICE_URL
          value: 'http://haproxy-load-balancer/'
        - name: MICROSERVICE_USERNAME
          value: microservice-prediction
        - name: MICROSERVICE_PASSWORD
          valueFrom:
              secretKeyRef:
                name: pumpwood-prediction
                key: microservice_password
