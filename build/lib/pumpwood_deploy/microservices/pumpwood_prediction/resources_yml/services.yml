apiVersion : "v1"
kind: Service
metadata:
  name: postgres-pumpwood-prediction
  labels:
    type: db
    db: postgres-pumpwood-prediction
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    type: db
    db: postgres-pumpwood-prediction
